import { describe, it } from 'vitest';

describe('excel.service', () => {
  it.todo('parseExcelBuffer returns Question[] with correct id, en, my fields from a valid workbook');
  it.todo('validateRow skips rows with blank questionId and excludes them from output');
});
