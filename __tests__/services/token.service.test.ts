import { describe, it } from 'vitest';

describe('token.service', () => {
  it.todo('generateToken returns a 64-char hex string');
  it.todo('generateToken is idempotent for the same email+surveyId combination');
});
